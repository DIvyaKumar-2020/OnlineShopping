import { Component, OnInit } from '@angular/core';
import { ActivatedRoute } from '@angular/router';
import { JhiDataUtils } from 'ng-jhipster';

import { IVegetables } from 'app/shared/model/vegetables.model';

@Component({
  selector: 'jhi-vegetables-detail',
  templateUrl: './vegetables-detail.component.html',
})
export class VegetablesDetailComponent implements OnInit {
  vegetables: IVegetables | null = null;

  constructor(protected dataUtils: JhiDataUtils, protected activatedRoute: ActivatedRoute) {}

  ngOnInit(): void {
    this.activatedRoute.data.subscribe(({ vegetables }) => (this.vegetables = vegetables));
  }

  byteSize(base64String: string): string {
    return this.dataUtils.byteSize(base64String);
  }

  openFile(contentType = '', base64String: string): void {
    this.dataUtils.openFile(contentType, base64String);
  }

  previousState(): void {
    window.history.back();
  }
}
